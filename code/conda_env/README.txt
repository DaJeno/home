If you need to install conda:
   ./install_and_config_conda.sh

# Relaunch terminal and execute the following to create the conda environment:
    ./create_conda_env.sh dev

# Activate the environment with:
    conda activate dev